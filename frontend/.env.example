# Copy to .env (local dev) and set the same value in Netlify → Site settings → Environment variables.
# The public URL of your Express API on Render, e.g.:
VITE_API_URL=https://sba2012-api.onrender.com
